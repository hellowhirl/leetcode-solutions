function isValid(s: string): boolean {
  return true;
}

class Stack {
  stack: string[];
  currentIndex: number;

  constructor() {
    this.stack = [];
    this.currentIndex = 0;
  }

  // push(val) {
  //   this.stack[this.currentIndex] = val;
  // }
}
